<%= f.input :language, as: :select, collection: Hyrax::LanguagesService.select_active_options,
    required: true, #todo this is out of whack with field setting (required: false) due to validation laxness required for import of legacy records
    input_html: { class: 'form-control', multiple: true } %>
