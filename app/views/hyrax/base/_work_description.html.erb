<% presenter.abstract.each do |abstract| %>
  <div class="ethos-abstract">
    <input type="checkbox" id="expanded"/>
    <p class="work_description"><%= iconify_auto_link(abstract) %></p>
      <button class="basic-button mdc-button mat-mdc-button mat-unthemed mat-mdc-button-base ethos-button tiny">
        <span class="mat-mdc-button-persistent-ripple mdc-button__ripple"></span>
        <span class="mdc-button__label"><label for="expanded" role="button">Read more</label></span>
      </button>
   </div>
<% end %>
